#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

# If you want to use GNOME config tools...
#
#if test -x /usr/lib/openbox/gnome-settings-daemon >/dev/null; then
#  /usr/lib/openbox/gnome-settings-daemon &
#elif which gnome-settings-daemon >/dev/null 2>&1; then
#  gnome-settings-daemon &
#fi

# If you want to use XFCE config tools...
#
#xfce-mcs-manager &


# monitor magic
autorandr --change
betterlockscreen --update ~/.dotfiles/fold.webp &
feh --bg-fill ~/.dotfiles/street.png

xss-lock --transfer-sleep-lock -- "betterlockscreen --lock" --nofork &
xautolock -time 10 -locker "idle-lock" &

nm-applet --no-agent &
cbatticon &

# TODO: use a different wallpaper
feh --bg-fill ~/.dotfiles/fold.webp

picom --daemon

playerctld daemon &

# conky &

tint2 &
# terminate instances of polybar that are already running
# killall -q polybar
# polybar &
# flameshot &

ibus-daemon -rxR &

discord --start-minimized --tray
