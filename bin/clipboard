#!/bin/sh

if [ $# -gt 0 ] && [ $# -lt 2 ] || [ -p /dev/stdout ]; then
  if [ $# = 1 ]; then
    xsel -ib <"$1"
    exit
  elif [ -p /dev/stdout ]; then
    xsel -ib
    exit
  fi
elif [ $# -eq 0 ]; then
  xsel -ob
else
  echo "echo \"text\" | clipboard" 1>&2
fi
